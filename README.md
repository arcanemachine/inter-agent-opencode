# Inter-agent for OpenCode

OpenCode extension for connecting agent sessions to the inter-agent message bus.

## Status

Initial implementation is planned but not yet available for installation. The initial host target is OpenCode `1.18.15`.

The extension will use direct TypeScript/Bun WebSocket connections with separate OpenCode TUI and server plugin targets. It will not require a Python runtime helper for routine operation.
